{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "start:webhook": "node dist/index-webhook.js",
    "build": "babel -d dist/ src/",
    "build:watch": "babel -w -d dist/ src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cycle/core": "^6.0.3",
    "body-parser": "^1.15.2",
    "cycle-express": "0.0.1",
    "cycle-telegram": "^2.0.0",
    "express": "^4.14.0",
    "ramda": "^0.21.0",
    "rx": "^4.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-plugin-transform-function-bind": "^6.8.0"
  }
}
